server:
  port: ${FOLLOW_SERVICE_PORT:8085}

spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_HOST:192.168.137.128}:8848
        namespace: ${NACOS_NAMESPACE:}
        group: DEFAULT_GROUP
