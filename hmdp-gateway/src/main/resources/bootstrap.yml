spring:
  application:
    name: hmdp-gateway
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_HOST:192.168.137.128}:8848
        namespace: ${NACOS_NAMESPACE:}
        group: DEFAULT_GROUP
        file-extension: yaml
  config:
    import:
      - nacos:hmdp-gateway.yaml?refresh=true
