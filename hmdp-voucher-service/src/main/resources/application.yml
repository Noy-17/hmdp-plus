server:
  port: ${VOUCHER_SERVICE_PORT:8083}

spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_HOST:192.168.137.128}:8848
        namespace: ${NACOS_NAMESPACE:}
        group: DEFAULT_GROUP
    sentinel:
      transport:
        port: 8721
