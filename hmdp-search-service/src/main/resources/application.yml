server:
  port: ${SEARCH_SERVICE_PORT:8086}

spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_HOST:192.168.137.128}:8848
        namespace: ${NACOS_NAMESPACE:}
        group: DEFAULT_GROUP
    sentinel:
      transport:
        port: 8725
